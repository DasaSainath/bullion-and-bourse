# Optional. Without this, the "why gold/silver moved" line on the dashboard
# falls back to a keyword heuristic over the live headlines — still works,
# just less sharp. Set this to get genuine AI-written one-line explanations.
# Get a key at https://console.anthropic.com
ANTHROPIC_API_KEY=
